{
  "buildCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install && pnpm --filter @workspace/file-share run build",
  "outputDirectory": "artifacts/file-share/dist/public",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
