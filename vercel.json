{
  "buildCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install && pnpm run build --filter @workspace/file-share",
  "outputDirectory": "artifacts/file-share/dist/public",
  "nodeVersion": "18.x",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
